/* OpenYog — see IDbDriver.h for dbDriverFor(); this header exists only so
 * the .cpp has a matching translation unit for the build's header/source
 * file-pair convention. */
#pragma once

#include "IDbDriver.h"
